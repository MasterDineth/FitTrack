import 'package:flutter/material.dart';

class FitnessProfileSetupScreen extends StatelessWidget {
  const FitnessProfileSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('FitnessProfileSetupScreen')));
  }
}
