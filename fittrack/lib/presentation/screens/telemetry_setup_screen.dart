import 'package:flutter/material.dart';

class TelemetrySetupScreen extends StatelessWidget {
  const TelemetrySetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('TelemetrySetupScreen')));
  }
}
